# Poisson 1D configuration (SGD)
DEBUGGING=true
PROJECT="debugging"
TRIALS=1
partition="debug"
time="00:10:00"

SCALING_TYPE="weak"
BATCH_SIZES=(64)
NUM_SUBD=(1)
NUM_STAGES=(1)
NUM_REP=(1)

USE_PMW=false
GRAD_ACC=false

OPTIMIZERS=(sgd)
DATASETS=(poisson1d)
MODELS=(pinn_ffnn)
NUM_LAYERS=()

EVAL_PARAMS=(epochs=1000 max_iters=0 criterion=pinn_poisson)

LEARNING_RATES=(0.01)
