# Allen-Cahn 1D configuration (APTS-PINN)
DEBUGGING=true
PROJECT="debugging"
TRIALS=1
partition="debug"
time="00:10:00"

SCALING_TYPE="strong"
BATCH_SIZES=(512)
NUM_SUBD=(1)
NUM_STAGES=(2)
NUM_REP=(1)

USE_PMW=false
GRAD_ACC=false

OPTIMIZERS=(apts_pinn)
DATASETS=(allencahn1d)
MODELS=(pinn_ffnn)
NUM_LAYERS=()

GLOB_SECOND_ORDERS=(false)
LOC_SECOND_ORDERS=(false)
GLOB_DOGLEGS=(false)
LOC_DOGLEGS=(false)

APTS_GLOB_OPTS=(tr)
APTS_LOC_OPTS=(tr)
FOC_OPTS=(false)

EVAL_PARAMS=(epochs=10 max_iters=0 criterion=pinn_allencahn)
APTS_PARAMS=(batch_inc_factor=0.0 overlap=0.0 glob_second_order=false)

LEARNING_RATES=()
