# MNIST configuration (APTS IP)
DEBUGGING=false
PROJECT="thesis_results"
TRIALS=1
partition="normal"
time="00:30:00"

SCALING_TYPE="strong"
BATCH_SIZES=(10000)
NUM_SUBD=(2)
NUM_STAGES=(1)
NUM_REP=(1)

USE_PMW=false
GRAD_ACC=false

OPTIMIZERS=(apts_d)
DATASETS=(mnist)
MODELS=(ffnn)
WIDTHS=(128)
NUM_LAYERS=()

GLOB_SECOND_ORDERS=(false)
LOC_SECOND_ORDERS=(false)
GLOB_DOGLEGS=(false)
LOC_DOGLEGS=(false)

APTS_GLOB_OPTS=(lssr1_tr)
APTS_LOC_OPTS=(lssr1_tr)
FOC_OPTS=(false)

EVAL_PARAMS=(epochs=10 max_iters=0 criterion=cross_entropy)
APTS_PARAMS=(batch_inc_factor=1.5 overlap=0.33 glob_second_order=false)
MAX_LOC_ITERS=2

LEARNING_RATES=()
