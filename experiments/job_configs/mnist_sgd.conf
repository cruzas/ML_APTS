# MNIST configuration (SGD sweep)
DEBUGGING=false
PROJECT="gamm2025sgd"
TRIALS=1
partition="normal"
time="01:00:00"

SCALING_TYPE="strong"
BATCH_SIZES=(10000)
NUM_SUBD=(1)
NUM_STAGES=(1)
NUM_REP=(1)

USE_PMW=false
GRAD_ACC=false

OPTIMIZERS=(sgd)
DATASETS=(mnist)
MODELS=(medium_ffnn)
WIDTHS=(8)
NUM_LAYERS=(2)

EVAL_PARAMS=(epochs=10 max_iters=0 criterion=cross_entropy)
APTS_PARAMS=(batch_inc_factor=1.0 overlap=0.0)

LEARNING_RATES=(0.1)
