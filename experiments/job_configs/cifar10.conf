# CIFAR-10 configuration (APTS-IP)
DEBUGGING=true
PROJECT="gamm2025debug2"
TRIALS=1
partition="debug"
time="00:30:00"

SCALING_TYPE="strong"
BATCH_SIZES=(200)
NUM_SUBD=(1)
NUM_STAGES=(1)
NUM_REP=(1)

USE_PMW=false
GRAD_ACC=false

OPTIMIZERS=(apts_ip)
DATASETS=(cifar10)
MODELS=(big_resnet)
NUM_LAYERS=()

GLOB_SECOND_ORDERS=(false)
LOC_SECOND_ORDERS=(false)
GLOB_DOGLEGS=(false)
LOC_DOGLEGS=(false)

APTS_GLOB_OPTS=(TR)
APTS_LOC_OPTS=(TRAdam)
FOC_OPTS=(false)

EVAL_PARAMS=(epochs=25 max_iters=0 criterion=cross_entropy)
APTS_PARAMS=(batch_inc_factor=1.5 overlap=0.33 glob_second_order=false)

LEARNING_RATES=()
