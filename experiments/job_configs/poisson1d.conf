# Poisson 1D configuration (APTS-D)
DEBUGGING=true
PROJECT="debugging"
TRIALS=1
partition="debug"
time="00:10:00"

SCALING_TYPE="weak"
BATCH_SIZES=(64)
NUM_SUBD=(2)
NUM_STAGES=(1)
NUM_REP=(1)

USE_PMW=false
GRAD_ACC=false

OPTIMIZERS=(apts_d)
DATASETS=(poisson1d)
MODELS=(pinn_ffnn)
NUM_LAYERS=()

GLOB_SECOND_ORDERS=(true)
LOC_SECOND_ORDERS=(true)
GLOB_DOGLEGS=(true)
LOC_DOGLEGS=(true)

APTS_GLOB_OPTS=(lssr1_tr)
APTS_LOC_OPTS=(lssr1_tr)
FOC_OPTS=(true)

EVAL_PARAMS=(epochs=1000 max_iters=0 criterion=cross_entropy)
APTS_PARAMS=(batch_inc_factor=1.5 overlap=0.33 glob_second_order=false)

LEARNING_RATES=()
