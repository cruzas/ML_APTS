# Tiny Shakespeare configuration (APTS-D)
DEBUGGING=false
PROJECT="gamm2025debug2"
TRIALS=1
partition="normal"
time="01:00:00"

SCALING_TYPE="strong"
BATCH_SIZES=(64)
NUM_SUBD=(1)
NUM_STAGES=(2)
NUM_REP=(1)

USE_PMW=true
GRAD_ACC=false

OPTIMIZERS=(apts_ip)
DATASETS=(tinyshakespeare)
MODELS=(nanogpt)
NUM_LAYERS=()

GLOB_SECOND_ORDERS=(false)
LOC_SECOND_ORDERS=(false)
GLOB_DOGLEGS=(false)
LOC_DOGLEGS=(false)

APTS_GLOB_OPTS=(TR)
APTS_LOC_OPTS=(TRAdam)
FOC_OPTS=(false)

EVAL_PARAMS=(epochs=5 max_iters=0 criterion=cross_entropy)
APTS_PARAMS=(batch_inc_factor=1.0 overlap=0.0 glob_second_order=false)

LEARNING_RATES=()
